name: Syntax Check
on: [push, pull_request]
jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ShellCheck
        run: |
          sudo apt install shellcheck -y
          shellcheck build*.sh *.sh 2>/dev/null || true
